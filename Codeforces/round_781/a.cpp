#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

void solve()
{
    ll x;
    cin >> x;
    ll ans = x - 3;
    cout << "1 " << ans << " 1 1\n";
    return;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int tt;
    cin >> tt;
    while (tt--)
        solve();
    return 0;
}